name: Hello Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello, Action"
      - name: Print Goodbye
        run: echo "Done, Game Over!"
